{
  "name": "aura-digital-store",
  "version": "5.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma generate && npx prisma db push --accept-data-loss && node index.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "telegraf": "^4.15.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
